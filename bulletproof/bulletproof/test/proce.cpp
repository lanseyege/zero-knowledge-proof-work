#include <stdio.h>
#include <sys/type.h>
#include <unistd.h>
#include <iostream>
#include <string>

using namespace std;

main () {
    pid_t pid;
    char *msg;
    int k;
    pid = fork();
    switch(pid) {
        
    }
}
